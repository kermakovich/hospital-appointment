<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE configuration
        PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>
    <properties resource="application.yml">
    </properties>
    <settings>
        <setting name="useGeneratedKeys" value="true"/>
            <setting name="logImpl" value="SLF4J"/>

    </settings>
    <environments default="development">
        <environment id="development">
            <transactionManager type="JDBC"/>
            <dataSource type="POOLED">
                <property name="driver" value="${spring.datasource.driver-class-name}"/>
                <property name="url" value="${spring.datasource.url}"/>
                <property name="username" value="${spring.datasource.username}"/>
                <property name="password" value="${spring.datasource.password}"/>
            </dataSource>
        </environment>
    </environments>
    <mappers>
        <mapper resource="mybatis/mapper/ReviewMapper.xml"/>
        <mapper resource="mybatis/mapper/UserInfoMapper.xml"/>
        <mapper resource="mybatis/mapper/PatientMapper.xml"/>
        <mapper resource="mybatis/mapper/DoctorMapper.xml"/>
        <mapper resource="mybatis/mapper/PatientCardMapper.xml"/>
    </mappers>
</configuration>